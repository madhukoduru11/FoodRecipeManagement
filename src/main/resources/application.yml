spring:
  datasource:
    username: root
    password: password
    url: jdbc:mysql://localhost:3306/foodcloud
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties.hibernate.dialect: org.hibernate.dialect.MySQL5Dialect
server:
  port: 9191